name: test
channels:
  - rdkit
dependencies:
    # Base depends
  - python
  - pip
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
     - rxn4chemistry
  #  - codecov

